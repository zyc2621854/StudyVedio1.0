<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.studyvedio">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:theme">
        <activity android:name=".ContactActivity"></activity>
        <activity android:name=".EditMessageActivity" />
        <activity android:name=".SearchDetailActivity" />
        <activity android:name=".EditNormalActivtiy" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".TestActivity9" />
        <activity android:name=".Study.mvp_dragger2.StudyActivity" />
        <activity android:name=".TestActivity8" />
        <activity android:name=".TestActivity7" />
        <activity android:name=".TextActivity6" />
        <activity android:name=".TestActivity5" />
        <activity android:name=".TestActivity4" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".TestActicvity2" >
        </activity>
        <activity android:name=".TestActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 这个 Demo 主要展示副单位的用法, 如果只使用副单位 (pt、in、mm) 就可以直接以像素作为单位填写设计图的尺寸, 不需再把像素转化为 dp -->
        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />
    </application>

</manifest>